@{
    ViewData["Title"] = "Facturación";
    Layout = "~/Views/Shared/_AdminLayout.cshtml";
}

    <link rel="stylesheet" href="~/css/views/facturacion/facturacion.css" />
    <link rel="stylesheet" href="~/css/views/facturacion/mobile.css" />
    <link rel="stylesheet" href="~/css/print-thermal.css" />





    <nav aria-label="breadcrumb" class="mb-3">
        <ol class="breadcrumb">
            <li class="breadcrumb-item">
                <a href="@Url.Action("Index", "Dashboard")" class="text-decoration-none">
                    <i class="bi bi-house-door me-1"></i>Dashboard
                </a>
            </li>
            <li class="breadcrumb-item active" aria-current="page">
                <i class="bi bi-cash-register me-1"></i>Facturación
            </li>
        </ol>
    </nav>

    <div class="container-fluid">
        <div class="row">
            <!-- Panel izquierdo - Productos y búsqueda -->
            <div class="col-lg-7">
                <div class="card shadow-sm">
                    <div class="card-header">
                        <h5 class="mb-0">
                            <i class="bi bi-search me-2"></i>Buscar Productos
                        </h5>
                    </div>
                    <div class="card-body">
                    <!-- Barra de búsqueda y filtros -->
                    <div class="row mb-3">
                        <div class="col-12">
                            <!-- Campo de búsqueda principal -->
                            <div class="input-group mb-2">
                                <span class="input-group-text">
                                    <i class="bi bi-search"></i>
                                </span>
                                <input type="text"
                                       id="busquedaProducto"
                                       class="form-control"
                                       placeholder="Buscar por nombre, marca, modelo"
                                       autocomplete="off">
                                <button class="btn btn-outline-secondary" type="button" id="btnToggleFiltros">
                                    <i class="bi bi-funnel"></i> Filtros
                                </button>
                            </div>

                            <!-- Panel de filtros colapsable -->
                            <div class="collapse" id="filtrosAvanzados">
                                <div class="card card-body py-2">
                                    <div class="row g-2">
                                        <!-- Filtro por Ancho -->
                                        <div class="col-md-3">
                                            <label class="form-label small fw-bold mb-1">
                                                <i class="bi bi-arrows-expand me-1"></i>Ancho
                                            </label>
                                            <select class="form-select form-select-sm" id="filtroAncho" multiple size="4">
                                                <option value="">Todos</option>
                                                <!-- Se llenará dinámicamente -->
                                            </select>
                                        </div>

                                        <!-- Filtro por Perfil -->
                                        <div class="col-md-3">
                                            <label class="form-label small fw-bold mb-1">
                                                <i class="bi bi-aspect-ratio me-1"></i>Perfil
                                            </label>
                                            <select class="form-select form-select-sm" id="filtroPerfil" multiple size="4">
                                                <option value="">Todos</option>
                                                <!-- Se llenará dinámicamente -->
                                            </select>
                                        </div>

                                        <!-- Filtro por Diámetro -->
                                        <div class="col-md-3">
                                            <label class="form-label small fw-bold mb-1">
                                                <i class="bi bi-disc me-1"></i>Diámetro (R)
                                            </label>
                                            <select class="form-select form-select-sm" id="filtroDiametro" multiple size="4">
                                                <option value="">Todos</option>
                                                <!-- Se llenará dinámicamente -->
                                            </select>
                                        </div>

                                        <!-- Filtro por Tipo de Terreno -->
                                        <div class="col-md-3">
                                            <label class="form-label small fw-bold mb-1">
                                                <i class="bi bi-geo-alt me-1"></i>Tipo Terreno
                                            </label>
                                            <select class="form-select form-select-sm" id="filtroTipoTerreno" multiple size="4">
                                                <option value="">Todos</option>
                                                <!-- Se llenará dinámicamente -->
                                            </select>
                                        </div>

                                        <!-- Filtro por Capas -->
                                        <div class="col-md-3">
                                            <label class="form-label small fw-bold mb-1">
                                                <i class="bi bi-layers me-1"></i>Capas
                                            </label>
                                            <select class="form-select form-select-sm" id="filtroCapas" multiple size="4">
                                                <option value="">Todas</option>
                                                <!-- Se llenará dinámicamente -->
                                            </select>
                                        </div>
                                    </div>

                                    <!-- Botones de acción -->
                                    <div class="row mt-2">
                                        <div class="col-12 text-end">
                                            <button type="button" class="btn btn-sm btn-outline-secondary" id="btnLimpiarFiltros">
                                                <i class="bi bi-x-circle me-1"></i>Limpiar Filtros
                                            </button>
                                            <button type="button" class="btn btn-sm btn-primary" id="btnAplicarFiltros">
                                                <i class="bi bi-check-circle me-1"></i>Aplicar Filtros
                                            </button>
                                        </div>
                                    </div>
                                </div>
                            </div>
                        </div>
                    </div>

                        <!-- Botones de navegación -->
                        <div class="row mb-3">
                            <div class="col-12">
                                <div class="botones-navegacion-facturacion">
                                    <button type="button" 
                                            id="btnFacturasPendientes" 
                                            class="btn btn-outline-warning"
                                            title="Ver todas las facturas"
                                            style="display: none;">
                                        <i class="bi bi-receipt me-2"></i>
                                        <span class="btn-text">Consultar Facturas</span>
                                    </button>

                                    <button type="button" 
                                            id="btnProformas" 
                                            class="btn btn-outline-success"
                                            title="Ver proformas">
                                        <i class="bi bi-file-earmark-text me-2"></i>
                                        <span class="btn-text">Proformas</span>
                                    </button>

                                    <a href="@Url.Action("EntregasPendientes", "Facturacion")" 
                                       class="btn btn-outline-info"
                                       title="Ver entregas pendientes">
                                        <i class="bi bi-truck me-2"></i>
                                        <span class="btn-text">Entregas Pendientes</span>
                                    </a>

                                    <button type="button" 
                                            id="btnServicios" 
                                            class="btn btn-outline-primary"
                                            title="Ver servicios disponibles">
                                        <i class="bi bi-tools me-2"></i>
                                        <span class="btn-text">Servicios</span>
                                    </button>

                                    <button type="button" 
                                            id="btnAbrirInventario" 
                                            class="btn btn-outline-secondary"
                                            title="Ver inventario completo">
                                        <i class="bi bi-grid-3x3-gap me-2"></i>
                                        <span class="btn-text">Inventario</span>
                                    </button>
                                </div>
                            </div>
                        </div>

                        <!-- Resultados de búsqueda -->
                        <div id="resultadosBusqueda" class="row">
                            <div class="col-12 text-center py-4 text-muted">
                                <i class="bi bi-search display-1"></i>
                                <p class="mt-2">Busca productos para agregar a la venta</p>
                            </div>
                        </div>
                    </div>
                </div>
            </div>

            <!-- Panel derecho - Carrito de venta -->
            <div class="col-lg-5">
                <div class="card shadow-sm">
                    <div class="card-header">
                        <h5 class="mb-0">
                            <i class="bi bi-cart me-2"></i>Venta Actual
                        </h5>
                    </div>
                    <div class="card-body">
                        <!-- Información del cliente -->
                        <div class="mb-3">
                            <label class="form-label fw-bold">
                                Cliente 
                                <span class="text-danger">*</span>
                                <small class="text-muted">(Requerido)</small>
                            </label>
                            <div class="input-group">
                                <input type="text" 
                                       id="clienteBusqueda" 
                                       class="form-control" 
                                       placeholder="Buscar cliente... (obligatorio)"
                                       autocomplete="off">
                                <button type="button" 
                                        id="btnNuevoCliente" 
                                        class="btn btn-outline-primary"
                                        title="Agregar nuevo cliente">
                                    <i class="bi bi-person-plus"></i>
                                </button>
                            </div>
                            <div id="clienteSeleccionado" class="d-none mt-2">
                                <small class="text-muted">Cliente seleccionado:</small>
                                <div class="alert alert-info py-2">
                                    <strong id="nombreClienteSeleccionado"></strong><br>
                                    <small id="emailClienteSeleccionado"></small>
                                </div>
                            </div>
                        </div>

                        <!-- Lista de productos en venta -->
                        <div class="mb-3">
                            <div class="d-flex justify-content-between align-items-center mb-2">
                                <label class="form-label fw-bold mb-0">Productos</label>
                                <span id="contadorProductos" class="badge bg-secondary">0 productos</span>
                            </div>

                            <div id="listaProductosVenta" class="productos-venta-container">
                                <div class="text-center py-4 text-muted">
                                    <i class="bi bi-cart-x display-4"></i>
                                    <p class="mt-2">No hay productos en la venta</p>
                                </div>
                            </div>
                        </div>

                        <!-- Totales -->
                        <div class="border-top pt-3">
                            <div class="row">
                                <div class="col-6">
                                    <strong>Subtotal:</strong>
                                </div>
                                <div class="col-6 text-end">
                                    <span id="subtotalVenta">₡0.00</span>
                                </div>
                            </div>
                            @* <div class="row">
                                <div class="col-6">
                                    <strong>IVA (13%):</strong>
                                </div>
                                <div class="col-6 text-end">
                                    <span id="ivaVenta">₡0.00</span>
                                </div>
                            </div> *@
                            <div class="row border-top pt-2">
                                <div class="col-6">
                                    <h5 class="mb-0"><strong>Total:</strong></h5>
                                </div>
                                <div class="col-6 text-end">
                                    <h5 class="mb-0 text-success">
                                        <strong id="totalVenta">₡0.00</strong>
                                    </h5>
                                </div>
                            </div>
                        </div>

                        <!-- Botones de acción -->
                        <div class="mt-3">
                            <div class="row">
                                <div class="col-6">
                                    <button type="button" 
                                            id="btnLimpiarVenta" 
                                            class="btn btn-outline-danger w-100"
                                            disabled>
                                        <i class="bi bi-trash me-1"></i>Limpiar
                                    </button>
                                </div>
                                <div class="col-6">
                                    <button type="button" 
                                            id="btnFinalizarVenta" 
                                            class="btn btn-success w-100"
                                            disabled>
                                        <i class="bi bi-check-circle me-1"></i>Finalizar
                                    </button>
                                </div>
                            </div>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </div>

    <!-- Modales -->
    @await Html.PartialAsync("_ModalInventario")
    @await Html.PartialAsync("_ModalDetalleProducto")
    @await Html.PartialAsync("_ModalFinalizarVenta")
    @await Html.PartialAsync("_ModalServicios")

<!-- Modal de Problemas de Stock -->
<div class="modal fade" id="problemasStockModal" tabindex="-1" aria-labelledby="problemasStockModalLabel" aria-hidden="true">
    <div class="modal-dialog modal-xl">
        <div class="modal-content">
            <div class="modal-header bg-warning text-dark">
                <h5 class="modal-title" id="problemasStockModalLabel">
                    <i class="bi bi-exclamation-triangle me-2"></i>Problemas de Stock Detectados
                </h5>
                <button type="button" class="btn-close" data-bs-dismiss="modal" aria-label="Close"></button>
            </div>
            <div class="modal-body">
                <div class="alert alert-warning">
                    <h6><i class="bi bi-info-circle me-2"></i>Información:</h6>
                    <p class="mb-0">Se han detectado productos con stock insuficiente en esta factura. Revisa los detalles y decide cómo proceder.</p>
                </div>

                <div id="problemasStockLoading" class="text-center py-4">
                    <div class="spinner-border text-warning" role="status">
                        <span class="visually-hidden">Verificando stock...</span>
                    </div>
                </div>

                <div id="problemasStockContent" style="display: none;">
                    <div class="row mb-3">
                        <div class="col-md-6">
                            <strong>Número de Factura:</strong><br>
                            <span class="text-primary fs-6" id="problemasStockFactura">-</span>
                        </div>
                        <div class="col-md-6">
                            <strong>Cliente:</strong><br>
                            <span id="problemasStockCliente">-</span>
                        </div>
                    </div>

                    <div class="table-responsive">
                        <table class="table table-hover table-striped">
                            <thead class="table-warning">
                                <tr>
                                    <th>Producto</th>
                                    <th class="text-center">Requerido</th>
                                    <th class="text-center">Disponible</th>
                                    <th class="text-center">Faltante</th>
                                    <th class="text-center">Acciones</th>
                                </tr>
                            </thead>
                            <tbody id="problemasStockTableBody">
                                <!-- Productos con problemas se cargan dinámicamente -->
                            </tbody>
                        </table>
                    </div>
                </div>
            </div>
            <div class="modal-footer">
                <button type="button" class="btn btn-secondary" id="btnCancelarProblemasStock" data-bs-dismiss="modal">
                    <i class="bi bi-x-circle me-1"></i>Cancelar
                </button>
                <button type="button" class="btn btn-warning" id="btnFacturarTodosModos" onclick="facturarTodosModos()">
                    <i class="bi bi-exclamation-triangle me-1"></i>Procesar de Todas Formas
                </button>
            </div>
        </div>
    </div>
</div>

<!-- Modal Facturas Pendientes -->
<div class="modal fade" id="facturasPendientesModal" tabindex="-1" data-bs-backdrop="static" data-bs-keyboard="false">
    <div class="modal-dialog modal-xl modal-fullscreen-lg-down">
        <div class="modal-content">
            <div class="modal-header bg-primary text-white">
                <h5 class="modal-title">
                    <i class="bi bi-receipt me-2"></i>
                    <span class="d-none d-md-inline">Consulta de Facturas</span>
                    <span class="d-inline d-md-none">Facturas</span>
                </h5>
                <button type="button" class="btn-close" data-bs-dismiss="modal"></button>
            </div>

            <div class="modal-body">
                <!-- Filtros de búsqueda -->
                <div class="filtros-factura-container">
                    <!-- Vista Desktop - Filtros horizontales -->
                    <div class="d-none d-md-block">
                        <div class="filtros-principales-desktop">
                            <!-- Campo de búsqueda -->
                            <div class="filtro-busqueda-factura">
                                <label class="form-label">
                                    <i class="bi bi-search me-1"></i>Buscar
                                </label>
                                <input type="text" 
                                       id="busquedaFacturasPendientes" 
                                       class="form-control" 
                                       placeholder="Número, cliente...">
                            </div>

                            <!-- Campo de estado -->
                            <div class="filtro-estado-factura">
                                <label class="form-label">
                                    <i class="bi bi-funnel me-1"></i>Estado
                                </label>
                                <select id="estadoFacturasPendientes" class="form-select">
                                    <option value="todos">Todos los Estados</option>
                                    <option value="Pendiente" selected>Pendiente</option>
                                    <option value="Pagada">Pagada</option>
                                    <option value="Anulada">Anulada</option>
                                    <option value="Vencida">Vencida</option>
                                </select>
                            </div>

                            <!-- Filtros de fecha -->
                            <div class="filtros-fechas-factura">
                                <div class="filtro-fecha-factura">
                                    <label class="form-label">Desde</label>
                                    <input type="date" id="fechaDesdeFacturas" class="form-control">
                                </div>
                                <div class="filtro-fecha-factura">
                                    <label class="form-label">Hasta</label>
                                    <input type="date" id="fechaHastaFacturas" class="form-control">
                                </div>
                            </div>

                            <!-- Botón limpiar -->
                            <div class="filtros-acciones-factura">
                                <button type="button" id="btnLimpiarFiltrosFacturas" class="btn">
                                    <i class="bi bi-arrow-clockwise"></i>
                                    <span class="d-none d-lg-inline">Limpiar Filtros</span>
                                    <span class="d-inline d-lg-none">Limpiar</span>
                                </button>
                            </div>
                        </div>
                    </div>

                    <!-- Vista Móvil/Tablet - Filtros verticales -->
                    <div class="d-block d-md-none">
                        <!-- Título de filtros móvil -->
                        <div class="filtros-titulo-mobile">
                            Filtros de Búsqueda
                        </div>

                        <!-- Campo de búsqueda -->
                        <div class="filtro-busqueda-factura">
                            <label class="form-label">
                                <i class="bi bi-search me-1"></i>Buscar
                            </label>
                            <input type="text" 
                                   id="busquedaFacturasPendientesMobile" 
                                   class="form-control" 
                                   placeholder="Número, cliente...">
                        </div>

                        <!-- Campo de estado -->
                        <div class="filtro-estado-factura">
                            <label class="form-label">
                                <i class="bi bi-funnel me-1"></i>Estado
                            </label>
                            <select id="estadoFacturasPendientesMobile" class="form-select">
                                <option value="todos">Todos los Estados</option>
                                <option value="Pendiente" selected>Pendiente</option>
                                <option value="Pagada">Pagada</option>
                                <option value="Anulada">Anulada</option>
                                <option value="Vencida">Vencida</option>
                            </select>
                        </div>

                        <!-- Filtros de fecha -->
                        <div class="filtros-fechas-factura">
                            <div class="filtro-fecha-factura">
                                <label class="form-label">Desde</label>
                                <input type="date" id="fechaDesdeFacturasMobile" class="form-control">
                            </div>
                            <div class="filtro-fecha-factura">
                                <label class="form-label">Hasta</label>
                                <input type="date" id="fechaHastaFacturasMobile" class="form-control">
                            </div>
                        </div>

                        <!-- Botones de acción -->
                        <div class="filtros-acciones-factura">
                            <button type="button" id="btnLimpiarFiltrosFacturasMobile" class="btn">
                                <i class="bi bi-arrow-clockwise"></i>
                                Limpiar Filtros
                            </button>
                        </div>
                    </div>
                </div>

                <!-- Loading -->
                <div id="facturasPendientesLoading" class="text-center py-5">
                    <div class="spinner-border text-primary" role="status">
                        <span class="visually-hidden">Cargando facturas pendientes...</span>
                    </div>
                    <p class="mt-2 text-muted">Cargando facturas pendientes...</p>
                </div>

                <!-- Contenido -->
                <div id="facturasPendientesContent" style="display: none;">
                    <!-- Tabla de Facturas Pendientes -->
                    <div class="table-responsive">
                        <table class="table table-hover table-sm">
                            <thead class="table-light">
                                <tr>
                                    <th style="min-width: 140px;">
                                        <span class="d-none d-md-inline">Número</span>
                                        <span class="d-inline d-md-none">N°</span>
                                    </th>
                                    <th style="min-width: 180px;">Cliente</th>
                                    <th style="min-width: 160px;" class="d-none d-lg-table-cell">
                                        Información
                                    </th>
                                    <th style="min-width: 120px;">Total</th>
                                    <th style="min-width: 100px;" class="d-none d-md-table-cell">
                                        Estado
                                    </th>
                                    <th style="min-width: 120px;" class="text-center">
                                        <span class="d-none d-sm-inline">Acciones</span>
                                        <i class="bi bi-gear d-inline d-sm-none"></i>
                                    </th>
                                </tr>
                            </thead>
                            <tbody id="facturasPendientesTableBody">
                                <!-- Se llena dinámicamente -->
                            </tbody>
                        </table>
                    </div>
                </div>

                <!-- Sin resultados -->
                <div id="facturasPendientesEmpty" style="display: none;" class="text-center py-5">
                    <i class="bi bi-inbox display-1 text-muted"></i>
                    <h5 class="mt-3 text-muted">No hay facturas pendientes</h5>
                    <p class="text-muted">Todas las facturas han sido procesadas o no coinciden con los filtros aplicados.</p>
                </div>

                <!-- Footer con paginación -->
                <div class="modal-pagination-footer mt-3">
                    <!-- Información y controles en móvil: vertical, en desktop: horizontal -->
                    <div class="d-flex flex-column flex-md-row justify-content-between align-items-center gap-3">
                        <!-- Info de resultados -->
                        <div class="pagination-info order-2 order-md-1">
                            <span id="facturasPendientesInfo" class="text-muted small">Mostrando 0 facturas</span>
                        </div>

                        <!-- Controles de paginación -->
                        <div class="pagination-controls d-flex flex-column flex-sm-row align-items-center gap-2 order-1 order-md-2">
                            <!-- Selector de cantidad por página -->
                            <div class="d-flex align-items-center">
                                <label for="facturasPendientesPorPagina" class="form-label me-2 mb-0 small d-none d-sm-inline">
                                    Por página:
                                </label>
                                <select id="facturasPendientesPorPagina" class="form-select form-select-sm" style="width: auto;">
                                    <option value="10">10</option>
                                    <option value="20" selected>20</option>
                                    <option value="50">50</option>
                                    <option value="100">100</option>
                                </select>
                            </div>

                            <!-- Navegación de páginas -->
                            <nav id="paginacionFacturas" style="display: none;">
                                <ul class="pagination pagination-sm mb-0">
                                    <!-- Se genera dinámicamente -->
                                </ul>
                            </nav>
                        </div>
                    </div>
                </div>
            </div>

            <div class="modal-footer">
                <button type="button" class="btn btn-secondary" data-bs-dismiss="modal">
                    <i class="bi bi-x-circle me-1"></i>Cerrar
                </button>
            </div>
        </div>
    </div>
</div>

<!-- Modal de Proformas -->
<div class="modal fade" id="proformasModal" tabindex="-1" aria-labelledby="proformasModalLabel" aria-hidden="true">
    <div class="modal-dialog modal-xl">
        <div class="modal-content">
            <div class="modal-header bg-success text-white">
                <h5 class="modal-title" id="proformasModalLabel">
                    <i class="bi bi-file-earmark-text me-2"></i>Proformas
                </h5>
                <button type="button" class="btn-close btn-close-white" data-bs-dismiss="modal" aria-label="Close"></button>
            </div>
            <div class="modal-body">
                <div class="alert alert-success">
                    <h6><i class="bi bi-info-circle me-2"></i>Información:</h6>
                    <ul class="mb-0">
                        <li><strong>Ver:</strong> Muestra los detalles de la proforma</li>
                        <li><strong>Imprimir:</strong> Genera un recibo de la proforma</li>
                        <li><strong>Convertir:</strong> Convierte la proforma en factura oficial</li>
                    </ul>
                </div>

                <!-- Filtros de Búsqueda -->
                <div class="card mb-3">
                    <div class="card-header bg-light">
                        <h6 class="mb-0">
                            <i class="bi bi-funnel me-2"></i>Filtros de Búsqueda
                        </h6>
                    </div>
                    <div class="card-body">
                        <div class="row g-3">
                            <!-- Búsqueda General -->
                            <div class="col-md-8">
                                <label for="busquedaProformas" class="form-label">
                                    <i class="bi bi-search me-1"></i>Búsqueda General:
                                </label>
                                <div class="input-group">
                                    <input type="text" 
                                           class="form-control" 
                                           id="busquedaProformas" 
                                           placeholder="Buscar por número, cliente, fecha..."
                                           autocomplete="off">
                                    <span class="input-group-text">
                                        <i class="bi bi-search"></i>
                                    </span>
                                </div>
                                <small class="text-muted">
                                    Busca en número de proforma, nombre del cliente, fecha de creación
                                </small>
                            </div>

                            <!-- Estado -->
                            <div class="col-md-4">
                                <label for="estadoProformas" class="form-label">
                                    <i class="bi bi-filter me-1"></i>Estado:
                                </label>
                                <select class="form-select" id="estadoProformas">
                                    <option value="todos">Todos los estados</option>
                                    <option value="Vigente">Vigente</option>
                                    <option value="Facturada">Facturada</option>
                                    <option value="Expirada">Expirada</option>
                                </select>
                            </div>
                        </div>

                        <!-- Botón Limpiar -->
                        <div class="row mt-3">
                            <div class="col-12 text-end">
                                <button type="button" class="btn btn-outline-secondary" id="btnLimpiarFiltrosProformas">
                                    <i class="bi bi-arrow-counterclockwise me-1"></i>Limpiar
                                </button>
                            </div>
                        </div>
                    </div>
                </div>

                <div id="proformasLoading" class="text-center py-4">
                    <div class="spinner-border text-success" role="status">
                        <span class="visually-hidden">Cargando proformas...</span>
                    </div>
                </div>
                <div id="proformasContent" style="display: none;">
                    <div class="d-flex justify-content-between align-items-center mb-3">
                        <h5 class="mb-0"><i class="bi bi-file-earmark-text me-2"></i>Proformas</h5>
                    </div>
                    <div class="table-responsive">
                        <table class="table table-hover table-striped">
                            <thead class="table-success">
                                <tr>
                                    <th>Número de Proforma</th>
                                    <th>Cliente</th>
                                    <th>Fecha y Usuario</th>
                                    <th>Total</th>
                                    <th>Estado</th>
                                    <th class="text-center">Acciones</th>
                                </tr>
                            </thead>
                            <tbody id="proformasTableBody">
                                <!-- Proformas se cargan dinámicamente -->
                            </tbody>
                        </table>
                    </div>
                </div>
                <div id="proformasEmpty" class="text-center py-4" style="display: none;">
                    <i class="bi bi-file-earmark-text text-muted" style="font-size: 3rem;"></i>
                    <h5 class="mt-3">No hay proformas</h5>
                    <p class="text-muted">No se encontraron proformas con los filtros aplicados</p>
                </div>

                <!-- Footer con paginación -->
                <div class="modal-pagination-footer d-flex justify-content-between align-items-center mt-3">
                    <div class="pagination-info">
                        <span id="proformasInfo" class="text-muted">Mostrando 0 proformas</span>
                    </div>

                    <div class="pagination-controls d-flex align-items-center">
                        <label for="proformasPorPagina" class="form-label me-2 mb-0">Proformas por página:</label>
                        <select id="proformasPorPagina" class="form-select me-3" style="width: auto;">
                            <option value="10">10</option>
                            <option value="20" selected>20</option>
                            <option value="50">50</option>
                            <option value="100">100</option>
                        </select>

                        <nav id="paginacionProformas" style="display: none;">
                            <ul class="pagination pagination-sm mb-0">
                                <!-- Se genera dinámicamente -->
                            </ul>
                        </nav>
                    </div>
                </div>
            </div>
            <div class="modal-footer">
                <button type="button" class="btn btn-secondary" data-bs-dismiss="modal">Cerrar</button>
            </div>
        </div>
    </div>
</div>

<!-- Modal para ingresar número de WhatsApp -->
<div class="modal fade" id="modalWhatsAppNumeroFacturacion" tabindex="-1" aria-labelledby="modalWhatsAppNumeroFacturacionLabel" aria-hidden="true" style="z-index: 1060;">
    <div class="modal-dialog">
        <div class="modal-content">
            <div class="modal-header bg-success text-white">
                <h5 class="modal-title" id="modalWhatsAppNumeroFacturacionLabel">
                    <i class="bi bi-whatsapp me-2"></i>
                    Compartir por WhatsApp
                </h5>
                <button type="button" class="btn-close btn-close-white" data-bs-dismiss="modal" aria-label="Close"></button>
            </div>
            <div class="modal-body">
                <!-- Preview del producto -->
                <div class="alert alert-info mb-3">
                    <h6 class="mb-2">Producto a compartir:</h6>
                    <div id="productoPreviewFacturacion">
                        <div class="text-center">
                            <div class="spinner-border spinner-border-sm me-2"></div>
                            Cargando información del producto...
                        </div>
                    </div>
                </div>

                <!-- Formulario de número -->
                <form id="formWhatsAppNumeroFacturacion">
                    <div class="mb-3">
                        <label for="numeroWhatsAppFacturacion" class="form-label fw-bold">
                            <i class="bi bi-phone me-1"></i>
                            Número de WhatsApp <span class="text-danger">*</span>
                        </label>
                        <div class="input-group">
                            <span class="input-group-text">+506</span>
                            <input type="tel" class="form-control" id="numeroWhatsAppFacturacion"
                                   placeholder="Ej: 88887777"
                                   pattern="[0-9]{8}"
                                   maxlength="8"
                                   required>
                        </div>
                        <div class="form-text">
                            <small class="text-muted">
                                <i class="bi bi-info-circle me-1"></i>
                                Ingrese solo los 8 dígitos del número (sin espacios ni guiones)
                            </small>
                        </div>
                    </div>

                    <div class="mb-3">
                        <div class="form-check">
                            <input class="form-check-input" type="checkbox" id="incluirImagenFacturacion">
                            <label class="form-check-label" for="incluirImagenFacturacion">
                                <i class="bi bi-image me-1"></i>
                                Incluir imagen del producto
                            </label>
                        </div>
                    </div>
                </form>
            </div>
            <div class="modal-footer">
                <button type="button" class="btn btn-secondary" data-bs-dismiss="modal">
                    <i class="bi bi-x-lg me-2"></i>Cancelar
                </button>
                <button type="button" class="btn btn-success" id="btnEnviarWhatsAppFacturacion">
                    <span class="normal-state">
                        <i class="bi bi-whatsapp me-2"></i>Enviar por WhatsApp
                    </span>
                    <span class="loading-state" style="display: none;">
                        <span class="spinner-border spinner-border-sm me-2"></span>
                        Enviando...
                    </span>
                </button>
            </div>
        </div>
    </div>
</div>

<!-- Toast Container -->
<div class="toast-container position-fixed bottom-0 end-0 p-3" style="z-index: 11"></div>

<style>
    /* Z-index para backdrop del modal de WhatsApp */
    .modal-backdrop.show:last-of-type {
        z-index: 1055 !important;
    }

    #modalWhatsAppNumeroFacturacion.show {
        z-index: 1060 !important;
    }
</style>

    @section Scripts {
        <!-- Scripts específicos para facturación -->
        <script src="~/js/utils/impresionTermica.js"></script>
        <script src="~/js/views/facturacion/zoomImagenes.js"></script>    
        <script src="~/js/views/facturacion/facturacion.js"></script>
        <script src="~/js/views/facturacion/serviciosFacturacion.js"></script>
        <script src="~/js/views/facturacion/facturasPendientesFilter.js"></script>
        <script src="~/js/views/facturacion/proformasFilter.js"></script>
        <script src="~/js/views/facturacion/InventarioFacturacion.js"></script>

    <script>
        // ===== CONFIGURACIÓN GLOBAL PARA FACTURACIÓN =====
        window.facturaConfig = @Html.Raw(Json.Serialize(ViewBag.ConfiguracionFacturacion));
        console.log('🎯 === CONFIGURACIÓN DE FACTURACIÓN CARGADA ===');
        console.log('🎯 Configuración completa:', window.facturaConfig);

        // ✅ VERIFICAR PERMISOS ESPECÍFICOS
        if (window.facturaConfig && window.facturaConfig.Permisos) {
            console.log('🔐 Permisos detectados:');
            console.log('🔐 - Puede crear facturas:', window.facturaConfig.Permisos.puedeCrearFacturas);
            console.log('🔐 - Puede completar facturas:', window.facturaConfig.Permisos.puedeCompletarFacturas);
            console.log('🔐 - Es administrador:', window.facturaConfig.Permisos.esAdmin);
        } else {
            console.error('❌ No se encontraron permisos en la configuración');
        }
    </script>

<!-- Configuración para JavaScript -->
    <script>
        window.facturaConfig = @Html.Raw(Json.Serialize(ViewBag.ConfiguracionFacturacion));
        console.log('🔧 === CONFIGURACIÓN DE FACTURACIÓN CARGADA ===');
        console.log('🔧 Configuración completa:', window.facturaConfig);
        console.log('🔧 Usuario disponible:', window.facturaConfig?.Usuario);
        console.log('🔧 Permisos disponibles:', window.facturaConfig?.Permisos);
        console.log('🔧 Token disponible:', window.facturaConfig?.TokenDisponible);
    </script>

    }