
<!-- Modal de Información de Permisos -->
<div class="modal fade" id="modalPermisosInfo" tabindex="-1">
    <div class="modal-dialog modal-lg">
        <div class="modal-content">
            <div class="modal-header">
                <h5 class="modal-title">
                    <i class="bi bi-info-circle me-2"></i>
                    Permisos Requeridos
                </h5>
                <button type="button" class="btn-close" data-bs-dismiss="modal"></button>
            </div>
            <div class="modal-body">
                <div id="permisosInfoContent">
                    <div class="alert alert-info">
                        <i class="bi bi-lightbulb me-2"></i>
                        <strong>¿No puedes acceder a una función?</strong> 
                        Aquí puedes ver qué permisos necesitas y solicitarlos al administrador.
                    </div>
                    
                    <div class="mb-3">
                        <label class="form-label">Selecciona la función:</label>
                        <select id="selectFuncion" class="form-select">
                            <option value="">-- Selecciona una función --</option>
                        </select>
                    </div>
                    
                    <div id="detallePermisos" style="display: none;">
                        <div class="card">
                            <div class="card-header">
                                <h6 class="mb-0">
                                    <i class="bi bi-shield-check me-2"></i>
                                    Permisos Requeridos
                                </h6>
                            </div>
                            <div class="card-body">
                                <div id="listaPermisosRequeridos"></div>
                                
                                <div id="accionesPermisos" class="mt-3">
                                    <div class="alert alert-warning">
                                        <i class="bi bi-exclamation-triangle me-2"></i>
                                        No tienes algunos de los permisos requeridos.
                                    </div>
                                    
                                    <div class="mb-3">
                                        <label class="form-label">Justificación para la solicitud:</label>
                                        <textarea id="justificacionSolicitud" class="form-control" rows="3" 
                                                placeholder="Explica por qué necesitas estos permisos..."></textarea>
                                    </div>
                                    
                                    <button id="btnSolicitarPermisos" class="btn btn-primary">
                                        <i class="bi bi-send me-2"></i>
                                        Solicitar Permisos al Administrador
                                    </button>
                                </div>
                            </div>
                        </div>
                    </div>
                </div>
            </div>
            <div class="modal-footer">
                <button type="button" class="btn btn-secondary" data-bs-dismiss="modal">Cerrar</button>
            </div>
        </div>
    </div>
</div>

<!-- Botón flotante para acceder a información de permisos -->
<div class="permisos-help-button">
    <button type="button" class="btn btn-info btn-floating" data-bs-toggle="modal" data-bs-target="#modalPermisosInfo" 
            title="Ver permisos requeridos">
        <i class="bi bi-question-circle"></i>
    </button>
</div>

<style>
.permisos-help-button {
    position: fixed;
    bottom: 20px;
    right: 20px;
    z-index: 1000;
}

.btn-floating {
    border-radius: 50%;
    width: 50px;
    height: 50px;
    display: flex;
    align-items: center;
    justify-content: center;
    box-shadow: 0 4px 8px rgba(0,0,0,0.2);
}

.permisos-categorias .categoria-group {
    border: 1px solid #dee2e6;
    border-radius: 8px;
    margin-bottom: 1rem;
}

.categoria-header {
    background-color: #f8f9fa;
    padding: 0.75rem 1rem;
    border-bottom: 1px solid #dee2e6;
    font-weight: 600;
    color: #495057;
}

.categoria-permisos {
    padding: 1rem;
    display: grid;
    grid-template-columns: repeat(auto-fill, minmax(250px, 1fr));
    gap: 0.5rem;
}

.permiso-item {
    display: flex;
    align-items: flex-start;
    gap: 0.5rem;
}

.permiso-item input[type="checkbox"] {
    margin-top: 0.25rem;
}

.permiso-label {
    font-size: 0.9rem;
    line-height: 1.3;
}

.permiso-description {
    font-size: 0.8rem;
    color: #6c757d;
    margin-top: 0.25rem;
}

.estado-permiso {
    display: inline-block;
    padding: 0.25rem 0.5rem;
    border-radius: 4px;
    font-size: 0.8rem;
    margin-left: 0.5rem;
}

.permiso-disponible {
    background-color: #d4edda;
    color: #155724;
}

.permiso-faltante {
    background-color: #f8d7da;
    color: #721c24;
}
</style>
<!-- Modal de Información de Permisos -->
<div class="modal fade" id="modalPermisosInfo" tabindex="-1" aria-labelledby="modalPermisosInfoLabel" aria-hidden="true">
    <div class="modal-dialog modal-lg">
        <div class="modal-content">
            <div class="modal-header">
                <h5 class="modal-title" id="modalPermisosInfoLabel">
                    <i class="bi bi-info-circle me-2"></i>
                    Información de Permisos
                </h5>
                <button type="button" class="btn-close" data-bs-dismiss="modal" aria-label="Close"></button>
            </div>
            <div class="modal-body">
                <div class="mb-3">
                    <label for="selectFuncion" class="form-label">Selecciona la Función:</label>
                    <select class="form-select" id="selectFuncion">
                        <option value="">-- Selecciona una función --</option>
                    </select>
                </div>

                <div id="detallePermisos" style="display: none;">
                    <div id="listaPermisosRequeridos"></div>
                    
                    <div id="accionesPermisos" style="display: none;">
                        <hr>
                        <div class="mb-3">
                            <label for="justificacionSolicitud" class="form-label">Justificación de la Solicitud:</label>
                            <textarea class="form-control" id="justificacionSolicitud" rows="3" 
                                      placeholder="Explica por qué necesitas estos permisos..."></textarea>
                        </div>
                        <button type="button" class="btn btn-primary" id="btnSolicitarPermisos">
                            <i class="bi bi-send me-2"></i>
                            Solicitar Permisos al Administrador
                        </button>
                    </div>
                </div>
            </div>
            <div class="modal-footer">
                <button type="button" class="btn btn-secondary" data-bs-dismiss="modal">Cerrar</button>
            </div>
        </div>
    </div>
</div>

<style>
.permiso-item {
    display: flex;
    align-items: center;
    padding: 8px 12px;
    margin-bottom: 8px;
    border-radius: 6px;
    background-color: #f8f9fa;
}

.permiso-disponible {
    background-color: #d1edff;
    color: #0c5460;
}

.permiso-faltante {
    background-color: #f8d7da;
    color: #721c24;
}

.estado-permiso {
    margin-left: auto;
    font-size: 0.85em;
    font-weight: 500;
    padding: 2px 8px;
    border-radius: 12px;
}
</style>
